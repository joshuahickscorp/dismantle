{
  "schema": "hawking.lab.experiment.v1",
  "id": "rescue_vocab_bench",
  "title": "training:rescue_vocab_bench.sh",
  "artifact_dir": "artifacts/runs/overnight",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/training/rescue_vocab_bench.sh",
    "framework_retired": "training_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/training/archive/rescue_vocab_bench.sh $LAB_HARNESS_ARGS\"; else bash tools/training/archive/rescue_vocab_bench.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
