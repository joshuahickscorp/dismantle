{
  "schema": "hawking.lab.experiment.v1",
  "id": "autotune_sweep",
  "title": "bench:autotune_sweep.sh",
  "artifact_dir": "artifacts/runs/autotune_sweep",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/autotune_sweep.sh",
    "framework_retired": "bench_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/bench/archive/autotune_sweep.sh $LAB_HARNESS_ARGS\"; else bash tools/bench/archive/autotune_sweep.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
