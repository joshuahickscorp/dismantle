{
  "schema": "hawking.lab.experiment.v1",
  "id": "launch_draft_sweep",
  "title": "training:launch_draft_sweep.sh",
  "artifact_dir": "artifacts/runs/launch_draft_sweep",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/training/launch_draft_sweep.sh",
    "framework_retired": "training_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/training/archive/launch_draft_sweep.sh $LAB_HARNESS_ARGS\"; else bash tools/training/archive/launch_draft_sweep.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
