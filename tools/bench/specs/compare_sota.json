{
  "schema": "hawking.lab.experiment.v1",
  "id": "compare_sota",
  "title": "bench:compare_sota.sh",
  "artifact_dir": "artifacts/runs/compare_sota",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/compare_sota.sh",
    "framework_retired": "bench_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/bench/archive/compare_sota.sh $LAB_HARNESS_ARGS\"; else bash tools/bench/archive/compare_sota.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
