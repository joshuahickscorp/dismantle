{
  "schema": "hawking.lab.experiment.v1",
  "id": "energy_paired",
  "title": "bench:energy_paired.sh",
  "artifact_dir": "artifacts/runs/energy_paired",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/energy_paired.sh",
    "framework_retired": "bench_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/bench/archive/energy_paired.sh $LAB_HARNESS_ARGS\"; else bash tools/bench/archive/energy_paired.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
