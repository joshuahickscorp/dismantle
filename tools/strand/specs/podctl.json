{
  "schema": "hawking.lab.experiment.v1",
  "id": "podctl",
  "title": "strand:podctl.sh",
  "artifact_dir": "artifacts/runs/podctl",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/strand/scripts/podctl.sh",
    "framework_retired": "strand_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/strand/archive/podctl.sh $LAB_HARNESS_ARGS\"; else bash tools/strand/archive/podctl.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
