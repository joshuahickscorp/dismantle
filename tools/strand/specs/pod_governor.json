{
  "schema": "hawking.lab.experiment.v1",
  "id": "pod_governor",
  "title": "strand:pod-governor.sh",
  "artifact_dir": "artifacts/runs/pod-governor",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/strand/scripts/pod-governor.sh",
    "framework_retired": "strand_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/strand/archive/pod-governor.sh $LAB_HARNESS_ARGS\"; else bash tools/strand/archive/pod-governor.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
