{
  "schema": "hawking.lab.experiment.v1",
  "id": "rss_monitor",
  "title": "bench:rss_monitor.sh",
  "artifact_dir": "artifacts/runs/rss_monitor",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/rss_monitor.sh",
    "framework_retired": "bench_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/bench/archive/rss_monitor.sh $LAB_HARNESS_ARGS\"; else bash tools/bench/archive/rss_monitor.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
