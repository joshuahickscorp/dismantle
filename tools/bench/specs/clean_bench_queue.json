{
  "schema": "hawking.lab.experiment.v1",
  "id": "clean_bench_queue",
  "title": "bench:clean_bench_queue.sh",
  "artifact_dir": "artifacts/runs/clean_bench_queue",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/clean_bench_queue.sh",
    "framework_retired": "bench_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/bench/archive/clean_bench_queue.sh $LAB_HARNESS_ARGS\"; else bash tools/bench/archive/clean_bench_queue.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
