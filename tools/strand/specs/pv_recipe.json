{
  "schema": "hawking.lab.experiment.v1",
  "id": "pv_recipe",
  "title": "strand:pv-recipe.sh",
  "artifact_dir": "artifacts/runs/pv-recipe",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/strand/scripts/pv-recipe.sh",
    "framework_retired": "strand_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/strand/archive/pv-recipe.sh $LAB_HARNESS_ARGS\"; else bash tools/strand/archive/pv-recipe.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
