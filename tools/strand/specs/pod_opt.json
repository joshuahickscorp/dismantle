{
  "schema": "hawking.lab.experiment.v1",
  "id": "pod_opt",
  "title": "strand:pod-opt.sh",
  "artifact_dir": "artifacts/runs/pod-opt",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/strand/scripts/pod-opt.sh",
    "framework_retired": "strand_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/strand/archive/pod-opt.sh $LAB_HARNESS_ARGS\"; else bash tools/strand/archive/pod-opt.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
