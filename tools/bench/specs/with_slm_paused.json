{
  "schema": "hawking.lab.experiment.v1",
  "id": "with_slm_paused",
  "title": "bench:with_slm_paused.sh",
  "artifact_dir": "artifacts/runs/with_slm_paused",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/with_slm_paused.sh",
    "framework_retired": "bench_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/bench/archive/with_slm_paused.sh $LAB_HARNESS_ARGS\"; else bash tools/bench/archive/with_slm_paused.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
