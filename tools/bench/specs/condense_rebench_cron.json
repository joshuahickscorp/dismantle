{
  "schema": "hawking.lab.experiment.v1",
  "id": "condense_rebench_cron",
  "title": "bench:condense_rebench_cron.sh",
  "artifact_dir": "artifacts/runs/condense_rebench_cron",
  "pause_flag": "artifacts/runs/PAUSE",
  "resume_flag": "artifacts/runs/RESUME",
  "meta": {
    "source_entrypoint": "tools/bench/condense_rebench_cron.sh",
    "framework_retired": "bench_shell_harness"
  },
  "stages": [
    {
      "id": "body",
      "shell": "if [ -n \"${LAB_HARNESS_ARGS:-}\" ]; then eval \"bash tools/bench/archive/condense_rebench_cron.sh $LAB_HARNESS_ARGS\"; else bash tools/bench/archive/condense_rebench_cron.sh; fi",
      "on_fail": "abort",
      "skip_if_done": false
    }
  ]
}
